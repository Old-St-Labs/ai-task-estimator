# DEPRECATED — use @orchestrator instead

This agent has been superseded by `orchestrator.md` (`@orchestrator`), which handles all general coding tasks directly in addition to planning and routing.

**Use `@orchestrator` for everything.**
